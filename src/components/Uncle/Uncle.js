import React from 'react';

const Uncle = ({house}) => {
    return (
        <div>
            <h4>Uncle is here</h4>
            <span>House: {house}</span>
        </div>
    );
};

export default Uncle;